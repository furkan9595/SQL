ALIAS : 

SQL takma adları, bir tabloya veya tablodaki bir sütuna geçici bir ad vermek için kullanılır.

Takma adlar genellikle sütun adlarını daha okunaklı hale getirmek için kullanılır.

Bir takma ad yalnızca bu sorgunun süresi boyunca mevcuttur.

AS Anahtar kelime ile bir takma ad oluşturulur .

ÖRNEK : 

SELECT U.NAMESURNAME ,U.EMAIL ,U.GENDER,U.BIRTDATE,A.[ADRESS+TEXT] 
FROM USERS U,ADRESS A WHERE U.ID=A.USER_ID AND U.ID=5
 
ÖRNEK : 

SELECT U.NAMESURNAME AS ADSOYAD ,
U.EMAIL EMAİL_ADRESİ ,
U.GENDER  CİNSİYET,
U.BIRTDATE DOGUM_TARİHİ,
A.[ADRESS+TEXT] ADRESİ
FROM USERS U,ADRESS A WHERE U.ID=A.USER_ID AND U.ID=7

ÖRNEK : 

SELECT U.NAMESURNAME AS ADSOYAD ,
U.EMAIL EMAİL_ADRESİ ,
U.GENDER  CİNSİYET,
U.BIRTDATE DOGUM_TARİHİ,
A.[ADRESS+TEXT] ADRESİ,
C.COUNTRY ULKE,
CI.CITY SEHİR
FROM USERS U,ADRESS A ,COUNTRIES C , CITIES CI
WHERE U.ID=A.USER_ID AND U.ID=5 AND C.ID=A.COUNTRY_ID AND CI.ID=A.CITY_ID


ÖRNEK : 
SELECT U.NAMESURNAME ADI_SOYADI, COUNT(A.ID) AS ADRES_SAYISI FROM USERS U ,ADRESS A GROUP BY U.NAMESURNAME

ÖRNEK :

SELECT U.NAMESURNAME ADI_SOYADI, COUNT(A.ID) AS ADRES_SAYISI,COUNT(DISTINCT CO.ID) ULKE 

FROM USERS U ,ADRESS A ,COUNTRIES CO GROUP BY U.NAMESURNAME


